from datetime import timezone, timedelta

# === Настройки ===

# Токен бота, полученный у @BotFather (тот же, что использует твой
# скрипт-создатель каналов, если он добавляет ИМЕННО этого бота в каналы)
BOT_TOKEN = "8641469804:AAFK5xovYZQwwkx0XM-KdEPYpyWcg7Ld1AQ"

# Текст поста по умолчанию (можно переопределить флагом --text при запуске)
DEFAULT_TEXT = "oh well whatever nevermind"

# Путь к картинке по умолчанию (можно переопределить флагом --image)
DEFAULT_IMAGE = "image.jpg"

# Файл, где хранится список каналов, куда бот был добавлен
CHANNELS_FILE = "channels.json"

# Файл, где хранится offset последнего обработанного апдейта Telegram
OFFSET_FILE = "offset.json"

# Пауза между отправками в разные каналы (сек) — чтобы не словить лимиты Telegram
DELAY_BETWEEN_CHANNELS = 1.0

# === Прокси (нужно только если api.telegram.org недоступен напрямую) ===
# На хостинге bothost.ru прокси не нужен — сервер напрямую достаёт до
# api.telegram.org, поэтому по умолчанию выключено. Если когда-нибудь
# понадобится (например, при переезде на другой хостинг с блокировкой
# Telegram), впиши сюда адрес прокси:
#   PROXY_URL = "socks5://127.0.0.1:10808"   # локальный SOCKS5 от VPN-клиента
#   PROXY_URL = "http://127.0.0.1:10809"     # локальный HTTP-прокси
PROXY_URL = None

# Если True — полностью игнорировать системные переменные окружения
# (HTTP_PROXY / HTTPS_PROXY / ALL_PROXY). Актуально только если что-то на
# хостинге само их выставляет и мешает прямому подключению; на bothost.ru
# обычно не нужно.
IGNORE_ENV_PROXY = False

# === Настройки постоянно работающего бота (bot.py) ===

# Telegram user id(-ы), которым разрешено писать боту и нажимать кнопки
# (/start, «Статус», «Запостить сейчас»). Всем остальным бот откажет.
# Узнать свой id можно, например, у @userinfobot.
ADMIN_IDS = [8241980244]  # <-- впиши сюда свой Telegram user id (можно несколько через запятую)

# Часовой пояс МСК. Это фиксированный UTC+3 (Россия с 2014 года не переводит
# часы), поэтому отдельная библиотека часовых поясов не нужна.
MSK_TZ = timezone(timedelta(hours=3))

# Время автопостинга по МСК: список пар (час, минута). Можно добавить ещё
# времена — просто допиши в список.
POST_TIMES = [(10, 0), (20, 0)]
